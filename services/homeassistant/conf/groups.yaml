misc:
  name: misc
  entities:
    - sun.sun
    - binary_sensor.iss

weather_yr:
  name: yr Weather
  entities:
    - sensor.weather_symbol
    - sensor.weather_temperature
    - sensor.weather_precipitation
    - sensor.weather_humidity

weather_camera:
  name: meteociel Cameras
  entities:
    - camera.morning
    - camera.afternoom
    - camera.tomorrowmorning
    - camera.tomorrowafternoom


servers:
  name: Servers
  entities:
    - sensor.consul_passing
    - sensor.consul_criticalwarning


links:
  name: Links
  entities:
    - weblink.google
    - weblink.my_computer

## VIEWS ##

#default_view:
#  name: Default
#  icon: mdi:home
#  view: yes
#  entities:
#    - group.servers
#    - group.misc
#    - group.links

weather:
  name: Weather
  view: yes
  entities:
    - group.weather_yr
    - group.weatehr_camera